[pytest]
testpaths = 
    promptsmith/tests
    promptsmith/tests/integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=promptsmith --cov-report=term-missing
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')